{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build & install restd",
            "type": "shell",
            "command": "./.vscode/vsbuild.sh -t 192.168.56.5 -m true",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
