{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build & install restd",
            "type": "shell",
            "command": "./.vscode/vsbuild.sh -t sdwan_address -m true",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
